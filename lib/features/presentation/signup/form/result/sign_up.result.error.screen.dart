import 'package:flutter/material.dart';

class SignupResultErrorScreen extends StatelessWidget {
  const SignupResultErrorScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}