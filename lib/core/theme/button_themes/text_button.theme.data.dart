import 'package:flutter/material.dart';

final textButtonLight = TextButton.styleFrom(
  textStyle: TextStyle(
    color: Colors.white,
  )
);
final textButtonDark = TextButton.styleFrom(
  textStyle: TextStyle(
    color: Colors.blue,
  )
);